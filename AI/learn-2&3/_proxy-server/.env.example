# 代理服务器环境变量配置

# 必需: 目标服务器地址
TARGET_URL=https://api.siliconflow.cn

# 可选: 代理服务器监听端口 (默认: 3000)
PORT=3000

# 可选: 日志目录 (默认: ./logs)
LOG_DIR=./logs

# 可选: 日志级别 (error, warn, info, debug - 默认: info)
LOG_LEVEL=info
